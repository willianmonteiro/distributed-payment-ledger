/**
 * Funds POST /accounts/:id/dev-seed — demo/dev tooling only, not part of the
 * public API. Kept separate from the interbank suspense account so demo
 * seeding never mixes into interbank-transfer-in-transit accounting.
 */
export const DEV_TREASURY_ACCOUNT_ID = '00000000-0000-0000-0000-000000000099';
